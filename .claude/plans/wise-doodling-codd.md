# SRS完了確認 ＋ HLD進捗管理の仕組み導入 ＋ テスト文書（02_test_policy.md・70_ST.md）新規作成

## Context

SRSがほぼ完成し、次のステージ（HLD）に進むにあたり3つの課題が持ち上がった。

0. **SRS完了の判定**: 「SRSが完了した」とみなす基準が曖昧だった。`mgmt/spec-findings/`には2026-06-29〜07-05にかけて`docs/20_SRS.md`の各章・各FRを対象にした詳細レビュー履歴が既に多数あり、直近の全体レビュー（`2026-07-05_0945_20_SRS.md`）時点の指摘は全て解消済みだった（3件の指摘が全て`fixed`または`no_change_needed`であることをファイル全文で確認済み）。ヘッダーのステータス欄には「FR-022純化・FR-023新設・N扱い再設計・phase番号統一」という記述が残っているが、`git log`で確認したところこれらは07-05レビューより前に個別のspec-panelサイクル（`FR-022・FR-023 spec-panel指摘①〜⑦対応`等、複数コミット）で既に反映・レビュー済みの内容であり、07-05の通しレビューにも含まれていた（新規指摘なし）。07-05以降に実際に変更されたのは`git log --since`で確認した2コミットのみで、うち1件は07-05レビュー自身の指摘対応（6bfceda）、もう1件が**§6.2.3（標高地形図の配色、ADR-SRS-047）**（d446a18）。つまり未レビューのまま残っているのは§6.2.3のみ。「spec-panelによる詳細レビューで指摘事項がなくなったこと」をSRS完了の判定基準とする
1. **HLDの進捗管理**: HLDは全体規模が大きく、着手前に「どれだけ出来たか」を把握する仕組みが無い。SRSで既に使われている「UR×FR/NFRトレーサビリティマトリクス」（`docs/20_SRS.md` §12）の考え方を踏襲し、FR/NFR単位で進捗を可視化する軽量な仕組みを作る。Fableサブエージェントによる検証を経て、粒度・ステータス定義・置き場所・archiving方針を確定した（詳細は下記「決定事項」）。
2. **URD対応テスト文書の要否**: SRC完成後に作る予定だったSTC（システムテストケース）は本来SRS（FR/NFR）を検証するものだが、V字モデルの標準的な対応ではURDにも対応するテスト文書（AT/受け入れテスト）が別途あるべきという指摘があった。個人開発規模（UR 13件）でAT文書を独立させると二重保守・形骸化リスクが高いため、Fableサブエージェントの検証を経て「STを第1部（SRS検証）・第2部（URD妥当性確認シナリオ）の2部構成にし、ATファミリは新設しない」という方針で合意した。この方針をADR化し、`docs/70_ST.md`をこの方針でSRSステージの作業として今のうちに新規作成する（HLD/LLD/CODの完了を待たない。ST はSRS/URDの内容を検証するものであり、要求が固まった時点でテストケースを起草できるため）。
3. **テスト方針書の欠落**（2026-07-12 ユーザー指摘で追加）: `70_ST.md`（テスト仕様書）はSTレベルのテストケースを定義するが、その上位にあるべきテスト方針（テストレベル定義・UT/IT/STの役割分担・自動/手動の分担・テスト環境・合否判定基準）を定める文書が計画に無かった。UT/IT/STの3ファミリを横断して律する常駐文書として `docs/02_test_policy.md`（00番台=共通・常駐）を新規作成する。70番台に置かない理由: ST専用に見え、将来の `50_UT.md`・`60_IT.md` からの参照が不自然になるため。

## 決定事項（会話内で既に合意済み）

- HLD進捗管理表は `mgmt/hld_progress.md` に置く（`docs/` ではなく devel専用の `mgmt/`。理由: SRSのRTMは「完成後も残る仕様の一部」だが、HLD進捗表は「HLD執筆中だけ意味を持つ作業管理アーティファクト」で性質が違うため）
- 粒度: FR/NFR単位の行を骨格にする。1つのFRに複数の設計論点（オープンなtype:設計issue等）が絡む場合は行を細分せず「関連issue」列で紐付ける。FRに還元できない横断章（コンポーネント構成、設定受け渡し方式、エラー処理方針等）は別途「章」種別の行を数件たてる
- ステータスは4段階: 未着手／ドラフト／レビュー中（spec-panel実施済み・ユーザー確認待ち、と入口条件を明記して形骸化を防ぐ）／確定
- 完了条件: 「確定」＝紐づくtype:設計issueが全クローズ＋該当章がHLD本文にドラフト済み
- 進捗表とGitHub issuesの二重管理を避けるため、進捗表の関連issue列は**参照のみ**（issue番号を書くだけ）。ステータスの正はissue側に置き、進捗表側に転記しない
- Archivingはしない（lessons.mdと違い有界なリストのため）。HLD完了時に本ファイルごと削除する（`.claude/plans` の完了時削除ルールと同じ扱い）
- `mgmt/CLAUDE.md` の新設は時期尚早と判断（今回は見送り）。凡例・運用ルールは `mgmt/hld_progress.md` 冒頭に自己記述する。将来LLD用の2表目が実際に生まれた時点で `docs/CLAUDE.md` に倣った `mgmt/CLAUDE.md` への昇格を検討する
- STは第1部（SRSのFR/NFR検証）・第2部（URDのUR妥当性確認シナリオ）の2部構成とする。ATファミリは新設しない。方針は `ADR-ST-001` に記録し、将来の再分離トリガー（例: 第三者受け入れ担当者の出現）を明記する
- テスト方針書は `docs/02_test_policy.md`（00番台=共通・常駐）に置く（2026-07-12 追加。placement理由はContext 3項参照）。`70_ST.md` のスコープ定義・合否判定は方針書を参照し、レベル定義・環境・完了基準を重複記述しない

## 対象ファイルと変更内容

### 0. SRS完了確認（ボトムアップ spec-panel 全件レビュー、方針変更）

**方針変更（2026-07-06、ユーザー指示）**: 当初は「07-05レビュー以降の未レビュー箇所（§6.2.3のみ）」に限定する予定だったが、既存レビュー履歴（section-group単位）の粒度に頼らず、TOC最下層から個別に・全件・指摘ゼロまで対応してから親階層へ昇る方式に変更する。

- 進捗管理: `mgmt/srs_review_progress.md` に全レビュー単位（Tier1〜Tier4）とステータスを記録し、セッションをまたいでも再開できるようにする
- Tier構成:
  - **Tier1（最下層、63件）**: FR-001〜FR-023（FR-010除く、22件）、NFR-001〜010（10件）、§6.2.1〜6.2.9（9件）、§7.2.1〜7.2.6（6件）、§8.2.1〜8.2.2（2件）、§2.2.1・§3.3.1・§3.3.2（3件）、§1・§9・§10・§11・§12（子要素を持たないため単独でTier1兼用、5件）
  - **Tier2（中間ロールアップ、12件）**: §2.2、§3.3、§4.1〜4.7（7件）、§6.2、§7.2、§8.2
  - **Tier3（上位ロールアップ、7件）**: §2、§3、§4、§5、§6、§7、§8（§1/§9/§10/§11/§12はTier1で完結のため対象外）
  - **Tier4（全体、1件）**: `docs/20_SRS.md` 全体の通しレビュー
- 進め方: 各ユニットに `/spec-panel <対象>` を実行 → 指摘のうち「作業実行」はその場で反映、「仕様検討」はユーザーに確認してから反映 → 指摘ゼロ（または対応不要確認済み）になったら進捗ファイルを更新して次のユニットへ。Tier1が全件完了してからTier2へ、Tier2が全件完了してからTier3へ、Tier3が全件完了してからTier4へ
- 完了条件: Tier4（全体通しレビュー）で指摘ゼロ、または残る指摘が全て「対応不要」とユーザー確認済みであること。その後ヘッダーの「ステータス」欄を `確定` のみに更新する
- 注記: 複数セッションにまたがる前提。中断する場合は `mgmt/srs_review_progress.md` の状態とhandoverで次セッションが再開できるようにする

### 1. `mgmt/hld_progress.md`（新規）

- 冒頭に凡例（種別: FR/NFR/章、ステータス4段階の入口条件、関連issue列の運用ルール＝参照専用、完了条件、archivingしない旨）を自己記述
- テーブル列: `#` / `種別` / `対象` / `関連issue` / `ステータス` / `備考`
- 行の内訳:
  - FR行 22件（`docs/20_SRS.md` の FR-001〜FR-023、FR-010はADR-SRS-028により削除済みのため対象外）
  - NFR行 10件（NFR-001〜NFR-010）
  - 章行（初期セット、実データ確認済みの「SRSがHLDに委譲した論点」を集約したもの）:
    - コンポーネント構成・データフロー詳細設計（SRS §3 のHLD版）
    - 設定受け渡し方式統一規約（config.ini/CLI引数/環境変数の使い分け。SRS §2.2.1 委譲分の集約）
    - エラー処理・exit code・リトライ/バックオフ方針の統一規約
    - CLI引数・メッシュ範囲指定の統一規約（issue #7 に対応。findsummits/merge.py/prefetch_tiles.py/run_all.sh 間で異なる引数仕様を統一する論点）
  - ログ出力の形式規約（ログファイル命名規則・設定キー名・保持期間・ローテーション方針）は独立した章行にせず、NFR-007行の備考欄に記載する（`docs/20_SRS.md` NFR-007 本文が既にこれらをHLD委譲事項として明記しており、独立章行にすると同一スコープの二重管理になるため）
  - 現時点で `jj1xgo/findsummits` の open issue のうち「対応予定ステージ: HLD」と明記されているもの（`gh issue view <N> --json body` で全 open issue を機械確認済み: #7, #10, #11, #12, #13, #15 の6件。**#9 は対応予定ステージが実際には LLD であり対象外**）を、内容を確認した上で対応するFR行・章行の関連issue列に割り当てる（例: #10/#11/#12/#13 → FR-013・FR-019行、#13/#15 → FR-020行、#15 → FR-018行、#7 → 上記「CLI引数・メッシュ範囲指定の統一規約」章行。実際の記載時に各issue本文と該当FRの入出力を再確認してから割り当てる）
- 全行「未着手」で初期化（作業自体はこの計画の対象外。次回以降のセッションでHLD執筆と並行して更新する）

### 2. `docs/02_test_policy.md`（新規、`00`番台=共通・常駐、テスト方針書）

- ヘッダー・目次は既存文書標準（`docs/CLAUDE.md`）に合わせる
- §1 目的・位置づけ: V字モデルと本プロジェクトの文書対応（UT↔LLD、IT↔HLD、ST↔SRS/URD。ST↔URDの扱いは `ADR-ST-001` を参照）
- §2 テストレベル定義と役割分担: UT/IT/ST 各レベルの対象・スコープ・検証しない範囲（例: UT=関数・モジュール単位〈C/Python〉、IT=コンポーネント間連携〈C出力→Python後処理のデータ受け渡し等〉、ST=FR/NFR・URレベルのエンドツーエンド検証）
- §3 テスト戦略: 自動テストと手動確認の役割分担（HTMLビューア等のWebUIは Claude in Chrome による手動確認、C/Pythonは自動テスト）、実データ依存（地理院タイル）の扱い（キャッシュ利用・モック方針・ネットワーク不要で再現できる範囲）
- §4 テスト環境: `01_environment.md` への参照を基本とし、テスト固有の前提のみ記述
- §5 合否判定・完了基準: 各レベルの exit criteria（カバレッジの考え方・既知欠陥の許容基準）
- §6 テストケースID命名の大枠: `ST-FR-001-01` 等の体系の共通規則（詳細テンプレートは各仕様書側に委ねる）
- 記述内容は URD/SRS/environment の実際の記載を都度参照しながら書く（推測で埋めない）

### 3. `docs/70_ST.md`（新規、`70`番台=STファミリ本体）

- ヘッダーは既存文書標準に合わせる（作成日/最終更新日/ステータス/参照SRS/参照URD）
- 目次
- §1 目的・スコープ: 第1部・第2部の役割分担、ADR-ST-001への参照。テストレベル定義・合否判定基準・環境は `02_test_policy.md` への参照で済ませ重複記述しない
- §2 テストケース記述規約: テストケースID命名規則（`02_test_policy.md` §6の共通規則に従う。例 `ST-FR-001-01`）、記述テンプレート（対象FR/NFR・前提条件・手順・期待結果・合否判定基準）
- 第1部: FR/NFR単位のシステムテストケース（FR-001〜FR-023〈FR-010除く〉、NFR-001〜NFR-010 それぞれに最低1テストケース。`docs/20_SRS.md`の各FRの「異常系」記述に対応する異常系テストケースも含める）
- 第2部: UR単位の妥当性確認シナリオ（UR-001〜UR-014〈UR-009除く〉。FRに一意に分解できるURはFR側テストケースへの参照でよいが、UR-001（全国176メッシュ実行）・UR-008（環境構築〜成果物生成の一気通貫）のようにFR横断・分解不能なURは専用のE2Eシナリオを記述する）＋ URカバレッジ表（`20_SRS.md` §12 と同形式）
- 記述内容はFR/NFR/URの実際の要求文言を都度参照しながら書く（推測で埋めない）

### 4. `docs/decisions/ADR-ST-001-two-part-st-no-separate-at-family.md`（新規）

- 状態: 採用・未実装 / 決定日: 2026-07-06
- Context: URD対応テスト文書の要否が問われた経緯、V字モデル標準（AT↔URD、ST↔SRS）との比較、個人開発規模（UR13件）での実務的トレードオフ。加えて、STは`docs/CLAUDE.md`の採番順（30-39 HLD→…→70-79 ST）より先に、SRSステージの作業として着手する点の理由も明記する（STはSRS/URDの確定内容を検証するものであり、HLD/LLD/CODの完了を待たずテストケースを起草できる。要求確定直後にテストケースを書くことで要求自体のテスト容易性も検証できるというV字モデルの一般的な実務慣行に基づく）
- Decision: STを2部構成にする。ATファミリは新設しない
- Alternatives:
  - STがURD対応も兼ねる（UR単純タグ付けのみ）: UR直接検証シナリオの欠落懸念により不採用
  - ATファミリ新設: 二重保守・形骸化リスクが高く、13件規模では見合わないため不採用
- Consequences: 将来SOTA日本支部以外の第三者受け入れプロセスが発生した場合はAT分離を再検討する（再分離トリガーとして明記）。なお、SOTA日本支部による申請受理は事実上の外部受け入れに相当するが、プロジェクト制御外のためテスト文書の対象にはできない旨も留保として明記する

### 5. `docs/00_GLOSSARY.md` の更新

- ADR STAGE列挙（9種）に `ST` は既に含まれているため変更不要（確認のみ）
- 略語表のST行「システムテスト（`docs/70_ST.md`、未作成）」は、本計画で`70_ST.md`を新規作成すると同時に「未作成」の注記が事実と食い違うため、**注記を削除する**（UT・IT行は引き続き未作成のため据え置き）
- `02_test_policy.md` の新規作成に伴い、GLOSSARY内の常駐ドキュメント一覧・略語表に行追加が必要かを実際のGLOSSARY構成を確認して判断し、必要なら追加する（2026-07-12 追加）

## 実行順序

1. `/spec-panel §6.2.3` → 指摘対応
2. `/spec-panel docs/20_SRS.md`（通しレビュー）→ 指摘ゼロを確認
3. `docs/20_SRS.md` ヘッダーの「ステータス」を `確定` に更新
4. `mgmt/hld_progress.md` 作成
5. `docs/02_test_policy.md` 作成（テスト方針書。2026-07-12 追加）
6. `docs/decisions/ADR-ST-001-...md` 作成
7. `docs/70_ST.md` 作成（ADR-ST-001・02_test_policy.mdを参照）
8. `docs/00_GLOSSARY.md` のST行「未作成」注記を削除＋`02_test_policy.md` 行追加要否の確認・対応
9. `make lint` で警告ゼロを確認
10. 完了後、ドキュメント更新のルール（CLAUDE.md）に従い、このターン内で個別に `git add` してコミット（pushはしない）

（本計画自体への `/spec-panel` セルフレビューはExitPlanMode前に実施済み。上記指摘は本計画に反映済み。手順1〜2の`/spec-panel`はSRS本文に対するレビューであり、本計画へのセルフレビューとは別物）

## 確認事項（spec-panelセルフレビューで判断保留とした点）

- `docs/70_ST.md` 第1部のテストケースで、NODATA・境界値・同値分割等のカバレッジ観点をどの粒度で明示するか（全FR一律で書くか、境界値依存の高いFR〈例: FR-003 NODATA処理、FR-006 プロミネンス閾値判定〉のみ複数ケースを厚めに書くか）は執筆時の判断が必要。本計画では後者（リスクの高いFRのみ複数ケース、他は代表ケース1件）を既定方針とする

## 検証方法

- `mgmt/spec-findings/` に手順1〜2それぞれの記録ファイルが実在することを確認する
- 手順2（通しレビュー）の記録ファイルで指摘が0件（または全件「対応不要」）であることを確認してから手順3（ステータス確定）に進む
- `docs/20_SRS.md` ヘッダーの「ステータス」欄が `確定` の一語のみになっていることを確認
- `make lint` で `docs/02_test_policy.md`・`docs/70_ST.md`・ADR・`docs/20_SRS.md`のMarkdown lint警告ゼロを確認
- `docs/70_ST.md` から `02_test_policy.md` への参照リンクが実在パスを指していることを確認（レベル定義・合否判定の重複記述が無いことも目視確認）
- `grep -n "FR-" docs/70_ST.md | wc -l` 等で全FR/NFR/URが最低1件のテストケース・シナリオでカバーされていることを機械確認
- `mgmt/hld_progress.md` の行数がFR22+NFR10+章4=36行以上であることを確認
- `git diff --stat` で意図したファイルのみが変更・新規作成されていることを確認
