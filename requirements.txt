requests
openpyxl
shapely
numpy
pillow

# lint（開発ツール: markdown チェック用。ランタイム import はしない。
# 再構築でルール挙動が変わらないようバージョン固定）
pymarkdownlnt==0.9.38
# lint（開発ツール: python 静的解析用。ランタイム import はしない。
# 再構築でルール挙動が変わらないようバージョン固定）
ruff==0.15.19
