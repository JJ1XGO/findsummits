# FR-004 出力テーブル是正 + 「解析範囲結合画像」改名

## Context

FR-004 第2回レビュー（ISSUE-089・反映済み）の継続確認で、ユーザーから 3 点の指摘を受けた：
1. 出力テーブルが「解析範囲結合画像」1 行のみで、解析対象メッシュ範囲情報・解析識別子が備考に押し込まれている → 独立した 3 行に分ける
2. 形式欄「メモリ上」はデータ型ではない（docs/CLAUDE.md ルール違反）→ 標高値の 2 次元配列に修正
3. 「解析範囲結合画像」という名前は誤解を招く（実体は画像ではなくデコード済み標高値のグリッド）→ **「解析範囲標高グリッド」に改名**（ユーザー選択済み）

## 修正内容（すべて `docs/20_SRS.md`）

### 1. 「解析範囲結合画像」→「解析範囲標高グリッド」改名（16 箇所）

- 対象行: L332, 346, 382, 385, 386, 389, 432, 433, 501, 507, 518, 525, 531, 591, 605, 623
- 一括置換でよいが、助数詞の不整合に注意:
  - L386「解析範囲結合画像 1 枚につき」→「解析範囲標高グリッド 1 件につき」
- 「各ピクセルの標高値」等の「ピクセル」表現は既存用語のまま維持（タイルのピクセルと 1:1 対応のため。改名スコープ外）

### 2. FR-004 出力テーブル（L343-346）を 3 行に分割・形式修正

| データ名 | 種別 | 形式 | 備考 |
|---|---|---|---|
| 解析範囲標高グリッド | 内部トランザクション | 標高値（浮動小数・m）の 2 次元配列 | 後段の [FR-015]/[FR-005]/[FR-006]/[FR-007]/[FR-016] に引き渡す |
| 解析対象メッシュ範囲情報 | 内部トランザクション | メッシュコード集合 + 緯度経度↔ピクセル位置対応 | 定義は説明参照。[FR-007] の地理的範囲フィルタ・[FR-016] の `area_complete` 判定が参照 |
| 解析識別子 | 内部トランザクション | 文字列（`3-<中心メッシュコード>`） | 定義は説明参照。[FR-015]/[FR-007]/[FR-016] の出力ファイル名の基底 |

- 形式欄「浮動小数（m）」は FR-003 出力の前例に揃える
- 各 FR 参照は既存どおり Markdown アンカーリンク

### 3. FR-007 入力テーブルに「解析対象メッシュ範囲情報」行を追加

説明文の地理的範囲フィルタが参照しているのに入力行がない。
`| 解析対象メッシュ範囲情報 | 内部トランザクション | 必須 | — | [FR-004] の出力。地理的範囲フィルタの範囲判定に使用 |`

### 4. FR-016 入力テーブルに「解析対象メッシュ範囲情報」行を追加

`area_complete` 判定が参照しているのに入力行がない。
`| 解析対象メッシュ範囲情報 | 内部トランザクション | 必須 | — | [FR-004] の出力。`area_complete` 判定に使用 |`

## 作業手順

1. ISSUE-089 を「対応中」に戻す（本修正は第2回レビューの追補。新規 issue は起こさない）
2. `docs/20_SRS.md` を編集（改名 → 出力 3 行化 → FR-007/FR-016 入力追加の順）
3. ISSUE-089 を再 close（追補内容をコメントに記載）
4. コミット（個別ファイル指定・Conventional Commits・本文日本語）

## 補足

- ISSUE-083（全 FR 形式欄見直し・低優先度）は「メモリ上」表記の全体掃除。今回の FR-004 是正はその先取りで、ISSUE-083 は残す
- FR-014 の出力テーブル詳細は次回 FR-014 レビューで扱う（今回の改名は FR-014 本文の用語にも及ぶが、それは単純置換）

## 検証

- `grep -n "解析範囲結合画像" docs/20_SRS.md` → 0 件
- `grep -n "メモリ上" docs/20_SRS.md` → FR-004 出力の表記が解消されていること（他 FR は ISSUE-083 スコープ）
- FR-004 出力 3 行と FR-005/FR-006/FR-007/FR-015/FR-016 入力行の名称・種別が一致していること
