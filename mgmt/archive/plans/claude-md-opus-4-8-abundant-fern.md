# 計画: グローバル CLAUDE.md への捏造防止セクション追加

## Context

Opus 4.8 + Claude Code で確認されている既知の挙動（ツール結果待機前に後続テキストを生成・捏造）
を防ぐためのガイドラインを、グローバル CLAUDE.md に恒久ルールとして明記する。
インシデントログや handover での反省知見を Claude 自身の行動規範に昇華させるのが目的。

## 変更対象

- `/home/node/.claude/CLAUDE.md` — ファイル末尾（`## ルールと制約` セクションの後）に追記

## 追記内容

ユーザー指定のテキストをそのまま追加（変更・省略なし）:

```text
## ツール出力の信頼性（捏造防止）
（以下、ユーザー提示テキスト全文）
```

## 検証

- 追記後にファイルを目視確認し、内容が欠損・文字化けなくセクション末尾に入っていることを確認
- グローバル CLAUDE.md はプロジェクトの `make lint` 対象外のため、lint 実行は不要
