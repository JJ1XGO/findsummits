# SRS 内部リンク破損修正

## Context

`docs/02_SRS.md` 内のアンカーリンクが 3 カテゴリ・計 9 箇所壊れている。

- **カテゴリ A (1 箇所)**: ISSUE-077 での 6.1 リネームが原因。旧タイトルのアンカーが残存。
- **カテゴリ B (1 箇所)**: FR-016 内の FR-009 参照。ヘッダーの完全な title と不一致（ISSUE-077 以前からの既存バグ）。
- **カテゴリ C (7 箇所)**: 6.6 ヘッダーのアンカー。`・` (U+30FB) が GitHub では除去されるが、リンク側が `html` を余分に含んだ誤った形になっている（ISSUE-077 以前からの既存バグ）。

URD の `<a id="...">` アンカーへのリンク (`01_URD.md#ur-001` 等) は GitHub GFM で正常に動作するため修正不要。

## 修正箇所

### カテゴリ A — 旧 6.1 タイトル (1 箇所)

| 行 | 現在 (broken) | 修正後 |
|---|---|---|
| 251 | `#61-入出力-地理院標高タイル取得ローカルキャッシュ` | `#61-入力-地理院標高タイルリモート取得元` |

### カテゴリ B — FR-009 不完全参照 (1 箇所)

| 行 | 現在 (broken) | 修正後 |
|---|---|---|
| 573 | `#fr-009-sota-リスト突合` | `#fr-009-sotaリスト突合match_status-判定` |

### カテゴリ C — 6.6 アンカー誤り (7 箇所)

全件同じ文字列を一括置換:

| 現在 (broken) | 修正後 |
|---|---|
| `#66-出力-geojsonhtml-作業用-html-ビューア` | `#66-出力-geojson作業用-html-ビューア` |

対象行: 69, 1333, 1334, 1335, 1336, 1443, 1444

## 修正対象ファイル

- `docs/02_SRS.md`（唯一の編集対象）

## 作業手順

1. カテゴリ A: 1 箇所の文字列置換
2. カテゴリ B: 1 箇所の文字列置換
3. カテゴリ C: 7 箇所を一括置換（`replace_all=true`）

## 検証

```bash
# 全破損パターンの残存なし確認
grep -n "61-入出力-地理院標高タイル取得\|fr-009-sota-リスト突合\b\|66-出力-geojsonhtml" /workspace/docs/02_SRS.md
# → 出力なし

# 修正後アンカーに対応するヘッダーが存在するか確認
grep -n "6\.1 入力.*リモート取得元\|FR-009: SOTAリスト突合.*match_status\|6\.6 出力: GeoJSON・作業用" /workspace/docs/02_SRS.md
# → 3行が存在すること
```

## コミット

- `docs(srs): 内部リンクの破損 3 件（計 9 箇所）を修正`
