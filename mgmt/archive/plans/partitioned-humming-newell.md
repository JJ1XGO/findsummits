# FR-005「ピーク候補検出」レビュー反映計画（ISSUE-087）

## Context

ISSUE-087 は SRS FR-005 のレビュー観点 A〜N（1 issue に 14 観点を詰めた旧形式）。
前提の ISSUE-075（FR-004/014 責務再編・オーケストレーション明示）は解決済み。
本計画はレビュー結果に基づき FR-005（docs/20_SRS.md 538-560 行）を修正する。

レビュー結論: **要修正 8 件（A/B/D/E/G/J/L/M）・閉じる 5 件（F/H/I/N/K）**。

## 修正対象

すべて `docs/20_SRS.md` の FR-005 セクション（538-560 行）。一部 GLOSSARY 連動。

### 観点 A — 対応 UR の修正
- 540 行 `UR-001, UR-002` → **`UR-001` 単独**に変更
- 理由: UR-002（プロミネンス150m抽出）は FR-006（計算）/FR-007（閾値）の責務。FR-005 は検出のみ
- 連動: SRS 冒頭の RTM 風対応表（173 行付近）で FR-005 の UR-002 ✅ を外す必要があれば合わせる

### 観点 B + L — 概要の中粒度化＋制御所在
- 541 行 概要を実装手順から WHAT レベルへ。FR-004/FR-014 から呼び出される被処理であることを示す
- 案: 「[FR-004]/[FR-014] が回す解析範囲標高グリッドの単一ピクセル走査の中で、局所的な地表面の最高点（ピーク候補）を検出する。」

### 観点 M — FR-006 との単一走査共有を本体に明示
- 説明欄（557-560 行）に 1 行追加: 「本処理とコル検出（[FR-006]）は同一の単一ピクセル走査ループを共有し、ピクセルごとに機能分担して実行する」

### 観点 D — 仕様抜けの補完（最重要）
説明欄に以下を追記:
1. **決定論性（最重要）**: 同一標高ピクセルのソートは決定論的なタイブレーク規則（例: ピクセルインデックス昇順）で順序を一意化し、解析結果の再現性を保証する
2. **NODATA 除外**: 無効値（-9999m）ピクセルは走査対象から除外（[FR-003] と整合）
3. **海面・1px ボーダー**: 海面（ELEV_SEA=0.0m）および FR-004 が付加した 1px 海面ボーダーの扱い（海面コンポーネントとして扱い、ピーク候補にはしない）

### 観点 E — 出力テーブル形式欄整理
- 553 行 形式欄「メモリ上。…引き継ぎ」→ データ型のみ（例: 「メモリ上のデータ構造」）。引き継ぎ先は備考欄へ移動（CLAUDE.md「形式欄はファイル形式・データ型のみ」準拠）

### 観点 G — 「山塊グループ」用語統一
- 558 行「山塊グループ」を既定義語「連結成分」へ統一（または GLOSSARY に定義追加）。
- 推奨: 用語統一（連結成分）。FR-006/Union-Find 文脈と一致

### 観点 J — アルゴリズム実装名の HLD 移管
- 553 行「Union-Find 構造を含む」・560 行「Union-Find（連結成分管理アルゴリズム）」から実装名 Union-Find を除き「連結成分管理」に手段非依存化
- FR-004 408 行の方針「Union-Find 等の実装アルゴリズムは HLD に記載」と整合させる

## 閉じる観点（修正不要・根拠）
- **F**: FR-005 内「ピーク候補」で統一済み・GLOSSARY 整合
- **H**: 異常系は FR-004（タイル不在）/FR-003（NODATA）で処理済み
- **I**: 入力欄 547 行「FR-004 または FR-014」既出
- **N**: 遷移は無条件順次・フロー図で明示済み
- **K**: ISSUE-084 で別管理

## 検証
- 修正後 FR-005 を通読し、概要が中粒度・制御所在が明確・決定論性が明記されているか確認
- 対応 UR が UR-001 単独になっているか
- 「Union-Find」「山塊グループ」が FR-005 から消え「連結成分」に統一されているか（grep）
- SRS 内の FR-005 参照リンク・RTM 対応表との整合

## ドキュメント反映後の手続き
- docs 更新につき即 commit（Conventional Commits・本文日本語）
- ISSUE-087 を `issue close`（対応完了）。観点 A〜N 13件中 8 修正・5 閉じを notes に記録
- GLOSSARY を変更する場合は同 commit に含める
