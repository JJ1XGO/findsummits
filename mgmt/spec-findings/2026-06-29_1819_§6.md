# spec-panel 指摘記録

- 対象: docs/20_SRS.md §6
- 日時: 2026-06-29

## 仕様検討・判断が必要な指摘

| 視点 | 重要度 | 指摘 | 該当箇所 | 推奨対応 | 対応先 |
|---|---|---|---|---|---|
| 仕様レビュアー / データ | 高 | §6.2.11 の使用カラムに `ValidTo` が含まれていない。JA の廃止済みサミット（ValidTo ≠ `31/12/2099`）が 147 件実在する。FR-009 説明にも廃止済みサミット除外の記述がなく、突合対象に廃止サミットが含まれると ①delete 判定ゾーン内 → match_status=delete → 申請書に誤った「削除」行が出力 ②判定ゾーン外 → unmatched 件数増加 → 不備ゲート誤発動のリスクがある | §6.2.11「使用カラム」/ FR-009「説明」冒頭 | 使用カラムに `ValidTo` を追記し、FR-009 の突合前処理として「`ValidTo` が実行日以降のサミットのみを対象とする」旨を明示する。除外条件の表現（実行日比較 vs `31/12/2099` 固定判定）を確認のうえ仕様化 | ISSUE-162 |
| アーキテクト | 中 | §6.1 No.10 の「使用/生成 FR」欄に FR-004/FR-014 が含まれているが、両 FR の入力テーブルでは「標高タイル（ローカルキャッシュ）」を「内部データ」として記載（外部I/F の記載なし）。ADR-SRS-016「ローカルキャッシュは内部データ」に基づくと §6.1 と FR 入出力テーブルが矛盾する | §6.1 No.10「使用/生成 FR」欄 / FR-004・FR-014 入力テーブル | ①§6.1 No.10 の「使用/生成 FR」を FR-001/FR-019 のみに修正、または ②FR-004/FR-014 の入力テーブルに「地理院標高タイル（外部I/F・キャッシュ再利用）」を追加する。ADR-SRS-016 と ADR-SRS-038 のどちらを上位原則とするかを決める必要がある | ISSUE-163 |

## 作業実行できる指摘

| 視点 | 重要度 | 指摘 | 該当箇所 | 推奨対応 | 対応先 |
|---|---|---|---|---|---|
| 仕様レビュアー | 中 | §6.2.1 のファイル名仕様（`ja0.geojson` 〜 `ja9.geojson`）と実在ファイル（`ref/geojson_v31/ja0_v31.geojson` 〜）が一致しない。実ファイルはバージョン番号サフィックス（`_v{N}`）が付く | §6.2.1「ファイル」欄 | §6.2.1 のファイル名記述を `ja0_v{N}.geojson` 〜 `ja9_v{N}.geojson` に修正する（ディレクトリ名の `{N}` と統一） | todo |
| 仕様レビュアー | 中 | FR-021 の入力テーブルに `merged_summit_revised.xlsx`（外部I/F・必須）が記載されているが、同 FR の説明に「ZIP 生成と同一操作内で FR-012 を内部実行して生成する」とある。内部実行で生成するものを「入力・外部I/F」として記載するのは ADR-SRS-016 の分類と矛盾する | FR-021 入力テーブル | 入力テーブルから `merged_summit_revised.xlsx` を削除し、説明文「FR-012 を内部実行して生成する」に一本化する（または「内部データ（FR-012 実行結果）」に種別変更する） | todo |
