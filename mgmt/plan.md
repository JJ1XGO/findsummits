# FR-004: メッシュ結合解析 レビューと修正

## Context

ISSUE-075（FR-004/FR-014 責務再編）が対応完了・verify 待ちの状態。再編後の FR-004（`docs/20_SRS.md` L329-384）を ISSUE-087 と同形式の観点リストでレビューし、修正を SRS に反映する。本レビューの完了が実質的な ISSUE-075 の verify を兼ねる。

## レビュー結果（観点リスト）

| 観点 | 内容 | 重要度 |
|---|---|---|
| A | **ADR-SRS-003 との矛盾**: ADR は「出力は中心メッシュ内のピークのみ」、SRS L379 は「解析対象メッシュ全体の地理的範囲内のピークを出力」。SRS が正（analysis_count 最大9回・FR-008 重複排除・lessons の expected_count 仕様と整合）。ADR-SRS-003 の Decision を更新 | 高 |
| B | **入力テーブル不足**: 「標高タイル（ローカルキャッシュ）」が入力に無い。7.2 一覧（L1449）では FR-004 が使用側、FR-002 入力も「FR-004 のタイル読み込みから渡される」とあり、FR-004 がタイル読み込み主体 → 入力に追加 | 高 |
| C | **誤参照**: L380「安定性を評価する（FR-009 参照）」→ analysis_count / expected_count / stability の定義・算出は FR-008。FR-008 に修正 | 中 |
| D | **ピーク出力範囲の記述所在**: L379 の per-mesh CSV 出力範囲（地理的範囲フィルタ＝フリンジ除外）は FR-007 の責務だが FR-007 に記述が無い。所在整理（FR-007 へ移すか相互参照） | 中 |
| E | **FR-015 呼び出しの漏れ**: 出力備考の引き渡し先には FR-015 があるが、概要・L381 のパイプライン列挙に FR-015 が無い。オーケストレーション記述に FR-015 のタイミング（タイル結合直後・FR-005 前）を含める | 中 |
| F | **「解析対象メッシュ範囲情報を伴う」が未定義**: 出力備考の語が何を指すか（メッシュコード集合？地理的範囲？）どこにも定義なし | 中 |
| G | **結合画像範囲の曖昧さ**: 「リストに存在するものを解析対象」と「リスト外 5140 は NODATA として解析」の関係が不明確。結合画像の範囲定義（解析対象メッシュの bounding box・隙間は NODATA 充填）を明文化 | 中 |
| H | **「全176メッシュ」ハードコード**: メッシュリストは可変入力。「メッシュコードリストの全メッシュ」等の表現に修正（NFR の性能目標での 176 使用は妥当・対象外） | 低 |
| I | **入力備考の整合**: 「1次メッシュコードリスト」備考が "—"。FR-001 同様 [7.1.1 参照] を付与 | 低 |
| J | **オーケストレーション役割の明示 + 処理フロー図**（ISSUE-087 観点 L の FR-004 側作業・ユーザー確認済みで本レビューに含める）: FR-004 説明に per-mesh パイプラインの処理順序（タイル結合・結合画像生成 → FR-015 地形図出力 → FR-005/006 単一走査 → FR-007 CSV 出力 → FR-016 ポリゴン生成）を明示し、処理フロー図を追加 | 高 |
| (対象外) | 出力テーブル「形式」欄の「メモリ上」表記 → ISSUE-083（全 FR 見直し）で対応済み登録 | — |

## 進め方（ユーザー合意済みの運用）

**ユーザー合意**: 観点 A〜J は 1 件ずつの確認を省略し、Fable の提案通りに今回まとめて修正する。次回セッションで修正後の FR-004 を改めてレビュー・修正する（それが実質の verify）。ISSUE-075・ISSUE-087 への注記・verify は **FR-004 と FR-014 両方のレビュー・修正が終わってから** 行う（今回は触らない）。

1. `track.py issue add` で「SRS FR-004 メッシュ結合解析のレビュー反映」を登録（観点リスト A〜J を説明に記載）→ `issue update --status 対応中`
2. SRS / ADR 修正:
   - `docs/20_SRS.md` FR-004 セクション（L329-384）+ 観点 D で FR-007 に地理範囲フィルタ記述追加
   - `docs/decisions/ADR-SRS-003-3x3-mesh-analysis.md`（観点 A: Decision の「出力は中心メッシュ内のピークのみ」を SRS 仕様に合わせ更新）
3. コミット（ドキュメント更新即時コミットルール・個別ファイル指定）
4. `issue close`（対応完了で止める。verify は次回セッションのレビュー後）
5. ISSUE-075 / ISSUE-087 は今回操作しない（FR-014 レビュー完了後にまとめて対応）

## 検証

- 修正後の FR-004 を通読し、FR-002/003/005/006/007/015/016・FR-008・7.2 一覧との相互参照が破綻していないことを grep で確認
- `grep -n "FR-004" docs/20_SRS.md` で参照箇所の整合確認
