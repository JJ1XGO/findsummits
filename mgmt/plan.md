# 計画: FR-022・FR-023 spec-panel 指摘①〜⑦ 対応

## Context

spec-panel レビューで FR-022（コル充足判定）と FR-023（解析パイプライン制御）に 7 件の問題を検出。
ISSUE-140〜142（設計判断が必要）として tracker 登録済み。残り 4 件は todo.md に追記済み。
変更対象ファイルは `docs/20_SRS.md` 1 本のみ。

## 設計決定（ISSUE resolution の確定）

| ISSUE | 決定内容 |
|---|---|
| ISSUE-140 | `key_col_unresolved_peaks-<N>.csv` カラム = `peak_lat, peak_lon` の 2 列（ヘッダー行あり・1行1件）。FR-014 は lat/lon から 1 次メッシュコードを計算できるためこれで十分 |
| ISSUE-141 | FR-022 異常系: (a) 入力 CSV 不存在 / 読み取りエラー → エラー終了（FR-023 に伝播）、(b) ピーク0件（空 CSV）→「ゼロ」返し・正常終了（フェーズ4 へ進む） |
| ISSUE-142 | FR-023 エラー伝播: 呼び出した FR がエラー終了した場合は FR-023 も即時終了（フェイルファースト方針。shell 実装の具体的手段は HLD 委任） |

## タスク一覧（全 Sonnet・対象: `docs/20_SRS.md`）

### T1: FR-023 入力テーブル + §7.1.1/§7.1.2.1 追記（todo①・高）

FR-023 入力テーブル（350行付近）の末尾行に追加:

```text
| 1次メッシュコードリスト | ユーザー入力 | 任意 | 日本全土1次メッシュコードリスト（全土） | [7.1.2.1 参照](#7121-1次メッシュコードリスト)。[FR-004](#fr-004-33メッシュ結合解析オーケストレーション)・[FR-008](#fr-008-per-mesh-csv-統合)・[FR-018](#fr-018-per-mesh-ピーク候補-geojson-統合) へ転送する |
```

§7.1.1 No.2「使用 FR」列（1608行）に `[FR-023](#fr-023-解析パイプライン制御)` を追記。
§7.1.2.1「使用 FR」行（1621行）にも同様に追記。

### T2: ISSUE-140 — `key_col_unresolved_peaks-<N>.csv` カラム定義追加

FR-022 出力テーブル（827行付近）の備考欄を更新:
`N = 生成段階タグ（3×3 後は \`-3\`、4×4 後は \`-4\`）。` の直後に
`カラム: \`peak_lat, peak_lon\`（ヘッダー行あり・1行1件）。` を挿入。

§7.2.1 No.7（1658行）「保存先 / 形態」列を更新:
`ファイル名: \`key_col_unresolved_peaks-<N>.csv\`` →
`ファイル名: \`$DATA_DIR/results/key_col_unresolved_peaks-<N>.csv\``

### T3: ISSUE-141 — FR-022 異常系追加

FR-022 説明末尾（「- 詳細・設計判断: [ADR-SRS-027]」の直前）に追加:

```markdown
**異常系**:

  - 入力 `merged_peak.csv` が存在しない、または読み取りエラーの場合はエラー終了する（[FR-023](#fr-023-解析パイプライン制御) に伝播）
  - ピーク件数がゼロ（空 CSV）の場合は未確定有無「ゼロ」を返して正常終了する（フェーズ4 へ進む）

```

### T4: ISSUE-142 + todo⑥⑦ — FR-023 説明に3箇所追記

**エラー伝播**（ループ終了条件 = 372行の直後）に追加:

```text
  - **エラー伝播**: 呼び出した FR がエラー終了した場合は FR-023 も即時終了する（フェイルファースト方針。`set -e` の使用等の具体的な実装手段は HLD に委ねる）
```

**各段の独立起動**（373行）を更新（フェーズ指定オプション + 前提条件を追記）:

```text
  - **各段の独立起動**: フェーズ2 / フェーズ3（各 N）/ フェーズ4 はそれぞれ単独で起動できる（フェーズ指定の起動オプションを提供する。具体的な構文は HLD で定義）。各段で中間成果物の目視確認が可能（自動ループは各段の動作が確認できてから利用することを推奨）。起動前提条件: フェーズ3 単独起動には `merged_peak.csv` と `key_col_unresolved_peaks-<前段N>.csv` の存在が必要。フェーズ4 単独起動には `merged_peak.csv` と `merged_peak.geojson` の存在が必要
```

## 実装手順

1. ISSUE-140〜142 を `issue update --status 対応中` にしてから実装開始
2. T1 → T2 → T3 → T4 の順に `docs/20_SRS.md` を編集
3. `make lint` 警告ゼロを確認
4. ISSUE-140〜142 を `issue close`
5. mgmt/todo.md の todo①⑤⑥⑦ を削除
6. `docs/20_SRS.md` と `mgmt/todo.md` をまとめて commit

## 検証

- `make lint` 警告ゼロ
- FR-023 入力テーブルに「1次メッシュコードリスト」行が存在する
- `key_col_unresolved_peaks-<N>.csv` の備考欄にカラム定義が存在する
- FR-022 に「異常系」セクションが存在する
- FR-023 説明に「エラー伝播」「前提条件」「フェーズ指定オプション」が存在する
